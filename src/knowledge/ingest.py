"""Ingest product/ICP/case-study docs (md, pdf, docx, txt) into structured
knowledge the value matrix can compile. Phase 1."""
from __future__ import annotations


def ingest(knowledge_dir: str) -> dict:
    raise NotImplementedError("Phase 1 — point at data/knowledge_base/")
