"""Fill segment templates per prospect: 4-5 touches (hook -> proof -> new angle
-> breakup), value retrieved from the matrix, hooks from enrichment signals.
Phase 3."""
from __future__ import annotations


def generate_sequence(prospect, matrix, templates) -> list[dict]:
    raise NotImplementedError("Phase 3 — needs matrix (Phase 1) + templates")
